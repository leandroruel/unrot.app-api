app = 'unrotapp'
primary_region = 'gru'

[build]

[env]
  SPRING_PROFILES_ACTIVE = 'prod'
  JAVA_OPTS = '-Xmx180m -Xms180m -XX:+UseSerialGC -XX:MaxMetaspaceSize=64m'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    interval = '15s'
    timeout = '5s'
    grace_period = '30s'
    method = 'GET'
    path = '/actuator/health'

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
