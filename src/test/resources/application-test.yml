spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false

app:
  security:
    jwt:
      secret: dGVzdHNlY3JldGtleXRoYXRpc2F0bGVhc3QzMmJ5dGVzbG9uZw==
      issuer: unrotapp
      ttl: 1h
  storage:
    seaweedfs:
      master-url: http://localhost:9333
